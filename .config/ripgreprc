# Search hidden files and directories.
--hidden

# Still, don't search .git directory
--glob=!.git/*

# Searches case insensitively if the pattern is all lowercase.
# Search case sensitively otherwise.
--smart-case
